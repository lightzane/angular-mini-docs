import { BypassHtmlPipe } from './bypass-html.pipe';

describe('BypassHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new BypassHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
